{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "GeoPulse: Debug active CMake target",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",
            "preLaunchTask": "GeoPulse: build"
        }
    ]
}
